# 网站代码测试容器 - MVP 基础版
# 用于验证生成项目的 TypeScript 编译与构建
# 用法: docker build -f docker/Dockerfile.test -t webecho-test .  (context = 生成的项目目录)
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
# 生成项目无 lock 文件，用 npm install；需 devDeps 用于 build
RUN npm install
COPY . .
RUN npm run build
